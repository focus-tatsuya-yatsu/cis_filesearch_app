{
  "name": "cis-filesearch-analyze-nas-path",
  "version": "1.0.0",
  "description": "Lambda function to analyze nas_path patterns in OpenSearch",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && cd dist && zip -r ../lambda.zip . && cd .. && zip -r lambda.zip node_modules",
    "deploy": "aws lambda update-function-code --function-name cis-filesearch-analyze-nas-path --zip-file fileb://lambda.zip",
    "test": "jest"
  },
  "dependencies": {
    "@opensearch-project/opensearch": "^2.5.0",
    "@aws-sdk/credential-provider-node": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.131",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
