# ========================================
# OpenSearch Configuration
# ========================================
#
# OpenSearch Endpoint - AWS OpenSearch Service URL
# 本番環境: https://vpc-cis-filesearch-xxxxx.ap-northeast-1.es.amazonaws.com
# ローカル開発環境でVPCエンドポイントを設定した場合、自動的にモックデータにフォールバックします
OPENSEARCH_ENDPOINT=https://search-cis-filesearch-xxxxx.ap-northeast-1.es.amazonaws.com
OPENSEARCH_INDEX=file-index

# ⚠️ ローカル開発環境の挙動:
# - VPCエンドポイント（vpc-で始まる）は自動検出され、モックデータを使用
# - パブリックエンドポイントの場合は実際のOpenSearchに接続を試行
# - 接続失敗時は自動的にモックデータにフォールバック

# ========================================
# AWS Configuration
# ========================================
AWS_REGION=ap-northeast-1

# AWS Credentials (for OpenSearch access)
# EC2インスタンス上で実行する場合はIAMロールを使用するため不要
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key

# ========================================
# S3 Configuration
# ========================================
S3_BUCKET=cis-filesearch-storage
S3_PROCESSED_PREFIX=processed/
S3_THUMBNAIL_PREFIX=thumbnails/

# ========================================
# Application Configuration
# ========================================
NODE_ENV=development
NEXT_PUBLIC_APP_NAME=CIS File Search Application
