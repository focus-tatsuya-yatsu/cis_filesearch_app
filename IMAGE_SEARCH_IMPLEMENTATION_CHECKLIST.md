# 画像検索機能 実装チェックリスト

**プロジェクト**: CIS File Search Application - 画像検索本番実装
**期間**: 2025-12-18 〜 2025-12-22 (5営業日)
**最終更新**: 2025-12-18

---

## 📋 Day 1: インフラ確認とバッチ処理準備 (2025-12-18)

### ✅ インフラ疎通確認

#### OpenSearch接続確認 (30分) 🔴 Critical
- [ ] VPC経由でOpenSearchエンドポイントに接続できるか確認
- [ ] 読み取りテスト（既存インデックスへのGETリクエスト）
- [ ] 書き込みテスト（テストドキュメントのPUT/DELETE）
- [ ] IAMロールの権限確認（es:ESHttpGet, es:ESHttpPut）

**確認コマンド:**
```bash
# 接続確認
curl -X GET "https://$OPENSEARCH_ENDPOINT/_cat/health?v"

# インデックス一覧
curl -X GET "https://$OPENSEARCH_ENDPOINT/_cat/indices?v"

# テスト書き込み
curl -X PUT "https://$OPENSEARCH_ENDPOINT/test-index/_doc/1" \
  -H "Content-Type: application/json" \
  -d '{"test": "data"}'

# テスト削除
curl -X DELETE "https://$OPENSEARCH_ENDPOINT/test-index"
```

---

#### Bedrock Titan Embeddings疎通確認 (30分) 🔴 Critical
- [ ] us-east-1リージョン設定確認
- [ ] IAM権限確認（bedrock:InvokeModel）
- [ ] テスト画像でベクトル生成テスト
- [ ] 1024次元ベクトルが正しく返ってくることを確認

**確認コマンド:**
```bash
# AWS CLI設定確認
aws configure get region  # us-east-1であること

# Bedrock利用可能なモデルリスト
aws bedrock list-foundation-models --region us-east-1

# テストベクトル生成（curlまたはPythonスクリプト）
cd /Users/tatsuya/focus_project/cis_filesearch_app/frontend
node -e "
const { BedrockRuntimeClient, InvokeModelCommand } = require('@aws-sdk/client-bedrock-runtime');
const fs = require('fs');

const client = new BedrockRuntimeClient({ region: 'us-east-1' });
const imageBytes = fs.readFileSync('test-images/sample.jpg');
const base64Image = imageBytes.toString('base64');

const command = new InvokeModelCommand({
  modelId: 'amazon.titan-embed-image-v1',
  contentType: 'application/json',
  accept: 'application/json',
  body: JSON.stringify({
    inputImage: base64Image
  })
});

client.send(command).then(response => {
  const result = JSON.parse(new TextDecoder().decode(response.body));
  console.log('Embedding dimensions:', result.embedding.length);
}).catch(err => console.error(err));
"
```

---

#### S3バケット接続確認 (20分) 🔴 Critical
- [ ] S3バケットへのアクセス確認
- [ ] 画像ファイルリスト取得テスト
- [ ] 画像ダウンロードテスト
- [ ] IAM権限確認（s3:GetObject, s3:ListBucket）

**確認コマンド:**
```bash
# バケット存在確認
aws s3 ls s3://cis-filesearch-thumbnails/

# 画像リスト取得（最初の10件）
aws s3 ls s3://cis-filesearch-thumbnails/thumbnails/ --recursive | head -10

# テスト画像ダウンロード
aws s3 cp s3://cis-filesearch-thumbnails/thumbnails/test.jpg /tmp/test.jpg

# ダウンロードした画像確認
file /tmp/test.jpg
```

---

#### 環境変数設定の完全確認 (15分) 🔴 Critical
- [ ] フロントエンド環境変数
- [ ] Lambda環境変数
- [ ] AWS認証情報

**確認項目:**

**フロントエンド (.env.local):**
```bash
# 必須環境変数
OPENSEARCH_ENDPOINT=vpc-cis-filesearch-opensearch-xuupcpgtq6a4opklfeh65x3uqe.ap-northeast-1.es.amazonaws.com
OPENSEARCH_INDEX=cis-files
AWS_REGION=ap-northeast-1
S3_BUCKET_NAME=cis-filesearch-thumbnails
NEXT_PUBLIC_API_URL=http://localhost:3000

# AWS認証（開発環境のみ）
AWS_ACCESS_KEY_ID=...
AWS_SECRET_ACCESS_KEY=...
```

**Lambda環境変数:**
```bash
# AWS Console または AWS CLI で確認
aws lambda get-function-configuration \
  --function-name cis-search-api \
  --query 'Environment.Variables'
```

---

#### バッチ処理スクリプトのドライラン (1時間) 🟡 High
- [ ] バッチ処理スクリプトの動作確認
- [ ] 10件の画像で正常動作テスト
- [ ] エラーハンドリング確認
- [ ] 進捗ログ確認

**実行手順:**
```bash
cd /Users/tatsuya/focus_project/cis_filesearch_app/frontend

# 環境変数設定
export OPENSEARCH_ENDPOINT="vpc-cis-filesearch-opensearch-xuupcpgtq6a4opklfeh65x3uqe.ap-northeast-1.es.amazonaws.com"
export OPENSEARCH_INDEX="cis-files"
export AWS_REGION="ap-northeast-1"
export S3_BUCKET_NAME="cis-filesearch-thumbnails"

# スクリプト編集: MAX_FILES = 10 に設定
# src/services/batch-process-images.ts の CONFIG.MAX_FILES = 10

# ドライラン実行
npx ts-node src/services/batch-process-images.ts

# 結果確認
# - 10件処理されたか
# - エラーが発生した場合、リトライされたか
# - OpenSearchにベクトルが保存されたか
```

**確認クエリ（OpenSearch）:**
```bash
# ベクトルが保存されたドキュメント数を確認
curl -X GET "https://$OPENSEARCH_ENDPOINT/cis-files/_count" \
  -H "Content-Type: application/json" \
  -d '{
    "query": {
      "exists": {
        "field": "image_embedding"
      }
    }
  }'
```

---

### 📦 Day 1 成果物

- [ ] インフラ疎通確認レポート
- [ ] 環境変数設定ドキュメント
- [ ] バッチ処理ドライラン結果
- [ ] 発見された問題点と対応策リスト

---

## 📋 Day 2: バッチ処理実行とLambda統合 (2025-12-19)

### ✅ バッチ処理本実行

#### バッチ処理実行（既存画像ベクトル化） (2-3時間) 🔴 Critical
- [ ] バッチ処理設定の最終確認
- [ ] 本番実行（全画像処理）
- [ ] 進捗モニタリング
- [ ] エラー対応

**実行手順:**
```bash
cd /Users/tatsuya/focus_project/cis_filesearch_app/frontend

# CONFIG設定を本番用に変更
# src/services/batch-process-images.ts
# MAX_FILES = 1000 (または実際の画像数)
# BATCH_SIZE = 10

# 本番実行
npx ts-node src/services/batch-process-images.ts 2>&1 | tee batch-process.log

# 別ターミナルで進捗確認
watch -n 30 'curl -s "https://$OPENSEARCH_ENDPOINT/cis-files/_count" \
  -H "Content-Type: application/json" \
  -d "{\"query\": {\"exists\": {\"field\": \"image_embedding\"}}}" | jq .'
```

**成功基準:**
- [ ] 95%以上の画像がベクトル化成功
- [ ] エラー率 < 5%
- [ ] 処理速度: 約10件/分

---

#### Lambda Search API CORS修正デプロイ (30分) 🔴 Critical
- [ ] CORS修正内容の最終確認
- [ ] Lambda関数デプロイ
- [ ] デプロイ後の動作確認

**実行手順:**
```bash
cd /Users/tatsuya/focus_project/cis_filesearch_app/backend/lambda-search-api

# CORS修正内容確認
# src/utils/error-handler.ts
# 'Access-Control-Allow-Methods': 'GET, POST, OPTIONS'

# デプロイ実行
chmod +x scripts/deploy-cors-fix.sh
./scripts/deploy-cors-fix.sh

# デプロイ確認
aws lambda get-function \
  --function-name cis-search-api \
  --query 'Configuration.LastModified'
```

---

#### Lambda KNN検索の動作確認 (1時間) 🔴 Critical
- [ ] テスト画像でベクトル生成
- [ ] Lambda経由で画像検索実行
- [ ] 検索結果の妥当性確認
- [ ] レスポンス時間測定

**テスト手順:**
```bash
# 1. テスト画像でベクトル生成
curl -X POST http://localhost:3000/api/image-embedding \
  -F "image=@test-images/sample.jpg" \
  > embedding-response.json

# 2. ベクトルを抽出
EMBEDDING=$(cat embedding-response.json | jq '.data.embedding')

# 3. 画像検索実行
curl -X POST http://localhost:3000/api/search \
  -H "Content-Type: application/json" \
  -d "{
    \"imageEmbedding\": $EMBEDDING,
    \"size\": 10,
    \"page\": 1
  }" | jq .

# 4. 結果確認
# - 検索結果が返ってくるか（0件でないこと）
# - レスポンス時間 < 3秒
# - relevanceScore が適切か（0.5以上が望ましい）
```

---

#### エラーハンドリング強化 (2時間) 🟡 High
- [ ] リトライロジックの実装/改善
- [ ] 詳細エラーログの追加
- [ ] タイムアウト設定の最適化

---

### 📦 Day 2 成果物

- [ ] バッチ処理完了レポート（成功率、エラーリスト）
- [ ] Lambda CORS修正デプロイ完了確認
- [ ] KNN検索動作確認レポート
- [ ] エラーハンドリング改善ログ

---

## 📋 Day 3: 統合テストとパフォーマンス最適化 (2025-12-20)

### ✅ 統合テストとパフォーマンス

#### End-to-End統合テスト (2時間) 🔴 Critical
- [ ] 画像アップロード → ベクトル生成 → 検索の全フロー
- [ ] 複数画像での動作確認
- [ ] エラーケースの確認

**テストケース:**
1. 正常系: 新規画像アップロード → 検索成功
2. 異常系: 非対応ファイル形式 → エラーハンドリング
3. 異常系: 大きすぎる画像 → エラーハンドリング
4. 正常系: フィルター付き検索
5. 正常系: ページング動作確認

---

#### パフォーマンステスト (1.5時間) 🟡 High
- [ ] レスポンス時間測定（平均、P50, P95, P99）
- [ ] 目標値達成確認（< 2秒）

**テストスクリプト:**
```bash
cd /Users/tatsuya/focus_project/cis_filesearch_app/frontend

# ベンチマーク実行
npm run benchmark:quick

# 結果確認
cat benchmark-results-*.json | jq '.summary'
```

---

#### エラー率測定 (1時間) 🟡 High
- [ ] 100回の検索実行
- [ ] エラー率 < 1% 確認

---

#### OpenSearch k-NN最適化適用 (1.5時間) 🟢 Medium
- [ ] HNSW設定最適化
- [ ] インデックス設定更新
- [ ] 検索速度改善確認

**実行手順:**
```bash
cd /Users/tatsuya/focus_project/cis_filesearch_app/backend/lambda-search-api

# 最適化適用
node scripts/apply-knn-optimization.js

# 効果確認（検索速度測定）
```

---

#### CloudWatch監視設定 (1時間) 🟢 Medium
- [ ] Lambda関数のメトリクス設定
- [ ] OpenSearchのメトリクス設定
- [ ] アラーム設定

---

### 📦 Day 3 成果物

- [ ] 統合テスト結果レポート
- [ ] パフォーマンステスト結果
- [ ] エラー率測定結果
- [ ] CloudWatch監視ダッシュボード

---

## 📋 Day 4: 本番デプロイ (2025-12-21)

### ✅ 本番環境デプロイ

#### Lambda関数本番デプロイ (1時間) 🔴 Critical
- [ ] 本番環境設定確認
- [ ] デプロイ実行
- [ ] 動作確認

---

#### フロントエンド本番デプロイ (1時間) 🔴 Critical
- [ ] ビルド実行
- [ ] 本番環境デプロイ
- [ ] 動作確認

---

#### 本番環境smoke test (1時間) 🔴 Critical
- [ ] 基本的な画像検索動作確認
- [ ] 主要機能の動作確認

---

#### ロードテスト（Artillery） (2時間) 🟡 High
- [ ] 50同時ユーザーでのテスト
- [ ] エラー率 < 1% 確認

---

#### 本番監視ダッシュボード確認 (30分) 🟡 High
- [ ] CloudWatchダッシュボード確認
- [ ] アラーム動作確認

---

### 📦 Day 4 成果物

- [ ] 本番デプロイ完了報告
- [ ] Smoke test結果
- [ ] ロードテスト結果
- [ ] 監視ダッシュボードURL

---

## 📋 Day 5: 最適化とドキュメント (2025-12-22)

### ✅ 最適化とドキュメント整備

#### キャッシング戦略実装 (2時間) 🟢 Medium
- [ ] LocalStorage + LRUキャッシュ実装
- [ ] キャッシュヒット率測定

---

#### 画像圧縮最適化 (1.5時間) 🟢 Medium
- [ ] クライアントサイド圧縮実装
- [ ] 画像サイズ削減効果確認

---

#### パフォーマンスベンチマーク (1.5時間) 🟢 Medium
- [ ] 本番環境でのベンチマーク実行
- [ ] レポート作成

---

#### 運用ドキュメント作成 (2時間) 🟡 High
- [ ] 運用手順書
- [ ] トラブルシューティングガイド

---

#### ユーザーガイド作成 (1時間) 🟢 Medium
- [ ] 画像検索機能の使い方ガイド

---

### 📦 Day 5 成果物

- [ ] 最適化実装完了報告
- [ ] パフォーマンスベンチマークレポート
- [ ] 運用ドキュメント
- [ ] ユーザーガイド

---

## 🎯 最終チェックリスト

### 機能要件
- [ ] 画像アップロード機能が動作
- [ ] ベクトル生成機能が動作（1024次元）
- [ ] OpenSearchへのベクトル保存が動作
- [ ] 画像類似検索が動作
- [ ] 検索結果が正しく表示される
- [ ] フィルター機能が動作
- [ ] ページング機能が動作

### パフォーマンス要件
- [ ] 画像アップロード→ベクトル化 < 2秒
- [ ] 画像検索レスポンス < 2秒
- [ ] バッチ処理速度: 10件/分以上
- [ ] 検索精度（Recall@10） > 90%
- [ ] エラー率 < 1%

### 可用性要件
- [ ] 画像検索API可用性 > 99%
- [ ] Lambda関数可用性 > 99.5%
- [ ] OpenSearch可用性 > 99.5%
- [ ] データ損失: 0件

### セキュリティ要件
- [ ] AWS認証が正しく設定されている
- [ ] IAM権限が最小権限の原則に従っている
- [ ] HTTPS通信が有効
- [ ] 入力検証が適切に実装されている

### ドキュメント要件
- [ ] 実装ガイドが完成
- [ ] 運用手順書が完成
- [ ] トラブルシューティングガイドが完成
- [ ] ユーザーガイドが完成

---

## 📊 成功指標

### 定量的指標
| 指標 | 目標値 | 実績値 | ステータス |
|-----|-------|-------|----------|
| 画像ベクトル化成功率 | > 95% | - | 🟡 |
| 検索レスポンス時間（平均） | < 2秒 | - | 🟡 |
| 検索レスポンス時間（P95） | < 3秒 | - | 🟡 |
| エラー率 | < 1% | - | 🟡 |
| 同時接続数 | 50ユーザー | - | 🟡 |

### 定性的指標
- [ ] ユーザーが直感的に使える
- [ ] エラーメッセージが分かりやすい
- [ ] パフォーマンスが快適
- [ ] ドキュメントが充実している

---

**担当PM**: Claude Code Product Manager
**最終更新**: 2025-12-18
